a=[1,2,3,4]
max=0
for i in a:
	if i>max:
		max=i
	if i<min:
		min=i
print max
print min
list=[]
num=int(input("how many number:"))
for n in range(num):
	numbers=int(input("enter the number"))
	list.append(numbers)
print list
print max(list)
print min(list)
			
		
